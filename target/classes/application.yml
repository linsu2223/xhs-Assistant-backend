server:
  port: 8080

spring:
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://127.0.0.1:3306/xhs_ai_rewriter?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:123456}
  sql:
    init:
      mode: always
      encoding: UTF-8

mybatis:
  configuration:
    map-underscore-to-camel-case: true
  type-aliases-package: com.xhs.rewriter.domain

app:
  jwt-secret: ${JWT_SECRET:xhs-local-dev-jwt-secret-at-least-32-bytes-2026}
  jwt-expire-hours: 24
  cookie-secret: ${COOKIE_SECRET:xhs-local-dev-cookie-secret-at-least-32-bytes}
  feishu:
    base-url: "https://jfsq6znqku.feishu.cn/base/GPSJbMfYzaty1Hs4yDCceDndnng?table=tblS8DhnLf3hfdTG&view=vewkmpmalY"
  deepseek:
    api-key: ${DEEPSEEK_API_KEY:sk-e5e19329b4c846e3ac8b941a1acba563}
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com/chat/completions}
    model: ${DEEPSEEK_MODEL:deepseek-chat}
